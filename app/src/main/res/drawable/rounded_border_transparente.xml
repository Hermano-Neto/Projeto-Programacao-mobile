<LayerList xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Forma com bordas arredondadas -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="20dp" /> <!-- Raio dos cantos -->
        </shape>
    </item>
    <!-- Imagem do botão -->
    <item android:drawable="@drawable/img_calculadora" />
</LayerList>
